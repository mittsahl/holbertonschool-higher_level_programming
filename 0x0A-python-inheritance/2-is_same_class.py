#!/usr/bin/python3
"""Function that checks if they data types are the same"""


def is_same_class(obj, a_class):
    """Function that checks if they are the same data type"""
    return type(obj) is a_class
