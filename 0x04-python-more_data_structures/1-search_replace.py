#!/usr/bin/python3
def search_replace(my_list, search, replace):
    new_list = my_list.copy()
    count = 0
    for item in new:
            if item  == search:
                new_list [count] = replace
            count += 1
    return new_list
